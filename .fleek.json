{
  "build": {
    "image": "wighawag/pnpm:latest",
    "command": "pnpm i --store=node_modules/.pnpm && pnpm run --filter {{=_.paramCase(it.name)}}-web ethers-module && pnpm run staging:web:build && pnpm run --filter {{=_.paramCase(it.name)}}-web fix_VITE_ENV",
    "publicDir": "web/dist"
  }
}
