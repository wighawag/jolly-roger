specVersion: 0.0.2
description: {{=name}}
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: {{=_.pascalCase(contractName)}}
    network: mainnet
    source:
      address: '{{contracts.{{=_.pascalCase(contractName)}}.address}}'
      abi: {{=_.pascalCase(contractName)}}Contract
      # startBlock: TODO
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.3
      language: wasm/assemblyscript
      entities:
        - Planet
      abis:
        - name: {{=_.pascalCase(contractName)}}Contract
          file: ./abis/{{=_.pascalCase(contractName)}}.json
      eventHandlers:
        - event: NameChanged(indexed address,string)
          handler: handleNameChanged
      file: ./src/mapping.ts